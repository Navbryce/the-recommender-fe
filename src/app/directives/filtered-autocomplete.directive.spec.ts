import { FilteredAutocompleteDirective } from './filtered-autocomplete.directive';

describe('FilteredAutocompleteDirective', () => {
  it('should create an instance', () => {
    const directive = new FilteredAutocompleteDirective(null);
    expect(directive).toBeTruthy();
  });
});
